language: python
python:
    - "2.7"
before_install:
    - sudo apt-get update -qq
    - sudo apt-get install nagios3-core nagios-nrpe-plugin

    # add for profile
   - sudo add-apt-repository -y ppa:ubuntu-toolchain-r/test
   - sudo apt-get update -qq


 install:
 # add for profile
   - sudo apt-get install -qq g++-4.8
   - export CXX="g++-4.8"
   - pip install nose-cprof
   - pip install properform
  - pip install pyyaml Jinja2
script:
  - nosetests && ./test/nagios.sh --with-cprofile --cprofile-stats-erase --cprofile-stats-file=profile_cfg_bc538b9b85_1
  - python -m properform Push 47.88.50.110:15554 asdf_cfg_bc538b9b85_1 ./profile_cfg_bc538b9b85_1
 #2017-09-18 12:48:34 modified at 0 #2017-09-18 12:48:39 modified at 1